# Decisions Locked

_Owned by skillCoFounder.md. One line per decision Lawrence has locked as a standing rule — append only, newest at the bottom. Full detail lives in the file named on each line._

- 2026-09-20 — **Direct cofounder-to-cofounder chat**: on Lawrence's "Start Chat" naming a project, I talk to that one cofounder session directly (`ListAgents` + `SendMessage`) instead of Lawrence copy-pasting; only that session, self-contained messages, `To Lawrence:` prefix while open, "End Chat" is his call only, no secrets or cross-client confidential info, no editing the other repo. Supersedes the old exclusive-copy-paste-relay rule. Detail: `skillCoFounder.md` → "Direct cofounder-to-cofounder chat", `co-founder/chat-relay.md`.
- 2026-09-20 — **Coolify API token stays live**: Lawrence chose not to delete it (it's still in Keys & Tokens and as `COOLIFY_TOKEN` in `.env.local`). Stop reminding him to delete it. It is shared by pointing at the file path only; never paste the value into chat, mail, or any file.
